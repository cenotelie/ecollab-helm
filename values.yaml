# Default values for ecollab.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

imageCredentials:
  registry: rg.fr-par.scw.cloud
  username: nologin
  password: xxx

versions:
  saas: git-d10df2f
  backend: git-84dbaf0
  frontend: git-0b9d28a
  activiti: 7.1.0.M5
  processes: git-a4512d7
  connectors: git-0cce581

rootUri: "https://integration.ecollab.cenotelie.fr"
